import React from "react"
import Image from "../components/image"

const Header = () => (
  <header>

      <div
      style={{
        display: `grid`,
        justifyItems: `center`,
      }}><Image /></div>

  </header>
)


export default Header
